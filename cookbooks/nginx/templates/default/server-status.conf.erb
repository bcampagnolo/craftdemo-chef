# nginx basic server-status
# only allow from localhost

server {
    listen <%= node[:nginx][:server][:server_status][:listen_port] %>;

    location /server-status {
       stub_status;
       allow 127.0.0.1;
       deny all;
    }
}